<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>

  <body>
    <%- include('partials/header') %> <%- include('partials/sidebar', { status:
    status, results: birds.length }) %>
    <main class="birdpage">
      <div class="birdpage-main">
        <div class="birdbox">
          <h1><%= bird.primary_name %></h1>
          <p id="eng-name">(<%= bird.english_name %>)</p>
          <div id="birdpage-img-container">
            <% const detailImgSrc = (/^https?:\/\//i.test(bird.filename)) ?
            bird.filename : ('/public/images/' + bird.filename); %>
            <img
              src="<%= detailImgSrc %>"
              alt="<%= bird.primary_name %>"
              id="birdpage-img"
            />
            <p class="photo-credit">© <%= bird.photographer %></p>
          </div>

          <ul class="birdpage-info">
            <li>
              <b>Scientific Name</b><span><%= bird.scientific_name %></span>
            </li>
            <li><b>Family</b><span><%= bird.family %></span></li>
            <li><b>Order</b><span><%= bird.order_name %></span></li>
            <li><b>Status</b><span><%= bird.status_name %></span></li>
            <li><b>Length</b><span><%= bird.length %> cm</span></li>
            <li><b>Weight</b><span><%= bird.weight %> g</span></li>
          </ul>
        </div>
        <div class="birdpage-buttons">
          <form action="/birds/<%= bird.bird_id %>/update" method="get">
            <button type="submit" class="button" id="edit-bird-button">
              Update
            </button>
          </form>

          <form
            id="delete-button"
            action="/birds/<%= bird.bird_id %>/delete"
            method="post"
          >
            <button type="submit" class="button" id="edit-bird-button">
              Delete
            </button>
          </form>
        </div>
      </div>
    </main>
    <%- include('partials/footer') %>

    <div id="confirmation-modal" class="modal">
      <div class="modal-content">
        <span class="close">&times;</span>
        <p>Are you sure you want to delete this bird?</p>
        <form id="delete-form" action="" method="post">
          <div class="delete-buttons">
            <button type="submit" class="button">Delete</button>
            <button type="button" class="button" id="cancel-button">
              Cancel
            </button>
          </div>
        </form>
      </div>
    </div>

    <script>
      document.addEventListener('DOMContentLoaded', () => {
        const modal = document.getElementById('confirmation-modal');
        const deleteForm = document.getElementById('delete-form');
        const closeButton = document.querySelector('.close');
        const cancelButton = document.getElementById('cancel-button');

        function openModal(formAction) {
          deleteForm.action = formAction;
          modal.style.display = 'block';
        }

        document
          .querySelectorAll('form[action*="/delete"] button[type="submit"]')
          .forEach((button) => {
            button.addEventListener('click', (event) => {
              event.preventDefault();
              const form = event.target.closest('form');
              openModal(form.action);
            });
          });

        closeButton.onclick = () => {
          modal.style.display = 'none';
        };

        cancelButton.onclick = () => {
          modal.style.display = 'none';
        };

        window.onclick = (event) => {
          if (event.target === modal) {
            modal.style.display = 'none';
          }
        };
      });
    </script>
  </body>
</html>
